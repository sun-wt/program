#include "mysplit.h"

int main()
{
	/*char buf[]="1 2      3  3  ";
	char *return_list[127];
	int size = 0;
	mystrsplit(return_list, &size, buf, NULL); 
	for(int i = 0;i < size; i++) 
		if(return_list[i] != NULL) printf("%s\n",return_list[i]);*/
    return 0;
}